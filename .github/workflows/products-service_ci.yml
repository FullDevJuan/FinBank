name: Products Service CI

on:
  push:
    branches: ["main"]
    paths:
      - "services/products-service/**"
      - "shared/**"
      - ".github/workflows/products-service_ci.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "services/products-service/**"
      - "shared/**"
      - ".github/workflows/products-service_ci.yml"

permissions:
  contents: read

jobs:
  products-service-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Usar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"
          cache-dependency-path: |
            services/products-service/package-lock.json
            shared/package-lock.json

      - name: Instalar dependencias compartidas
        working-directory: ./shared
        run: npm ci

      - name: Instalar dependencias del products-service
        working-directory: ./services/products-service
        run: npm ci

      - name: Ejecutar tests del products-service
        working-directory: ./services/products-service
        env:
          DATABASE_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: npm test
